# Archives

Frozen snapshots kept for reference, comparison, and local replay — not part of the active app build.

| Path | Contents |
|------|----------|
| [`v2.3-web/`](v2.3-web/) | Production Flutter web build from the **v2.3** line (~app `0.0.41+41`) |

Sync to GitHub Pages with `scripts/sync_legacy_web_to_docs.sh`, then preview the full site with `scripts/preview_web.sh` (see each archive's README).
